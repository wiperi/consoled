repos:
  - repo: https://github.com/pycqa/flake8
    rev: 7.1.1
    hooks:
      - id: flake8
        args: ['--max-line-length=120']

  # - repo: local
  #   hooks:
  #     - id: sync-downstream
  #       name: Sync downstream repositories
  #       entry: ./sync_downstream.sh
  #       language: system
  #       pass_filenames: false
  #       always_run: true

  # - repo: local
  #   hooks:
  #     - id: run-tests
  #       name: Run console-monitor tests
  #       entry: bash -c 'cd downstream/sonic-host-services && python -m pytest tests/console_monitor/ -v'
  #       language: system
  #       pass_filenames: false
  #       always_run: true
