# 启动阶段

根据CONFIG_DB中的值，在管理串口上启用DCE功能。

检测自己是否启用了受控端口，如果启用了，则在受控端口上启动DTE的功能。

# DCE

引入一个Proxy，独占串口的读写权限，负责与DTE通信，并将数据过滤后转发给上层应用。

当Proxy收到心跳包时，更新心跳计时器，Oper 状态置为UP。过滤掉心跳包，不转发给上层应用。

对于其他数据，直接转发给上层应用。

# DTE

周期向串口发送心跳

# 心跳

方案1：使用单个非打印字符作为心跳包
好处：稳定
坏处：该字符不能作为普通数据发送

方案2：使用特定的非打印的字符串作为心跳包